# brashandplucky.github.io-jekyll
Jekyll sources for brashandplucky.github.io

Everything is set so calling `jekyll serve` will deploy the files
in the `brashandplucky.github.io` repository, leaving it ready
for `git push`.